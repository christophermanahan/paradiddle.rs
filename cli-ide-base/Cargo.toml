[package]
name = "cli-ide-base"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
crossbeam = "0.8"
serde = { version = "1.0", features = ["derive"], optional = true }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "event_bench"
harness = false